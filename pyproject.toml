[project]
name = "salary-calculator-cli"
version = "0.1.0"
description = "CLI for normalizing salaries across time units and currencies"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.3",
]
